<?php

use ToolsBundle\Tests\WamowTestCase;

class TestDaMissionTest extends WamowTestCase
{
    // need v2 wrk exp
    public function datKindOfTest()
    {
        // we get 2 specific user
        // we create a mission
        // we make the matching
        // we ensure user are matched
        // we try to select them -> not enough message
        // we send message
        // we pass
        // we pass
        // one win
        self::assertTrue(true);
    }

    public function kronosGodOfTime()
    {
        // we get 2 user
        // we create mission
        // we play on time for status
        // end's
        self::assertTrue(true);
    }
}