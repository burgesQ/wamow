{% extends '::base.html.twig' %}
{% trans_default_domain 'FOSUserBundle' %}

{% block body %}
    <div id="wrapper">
        <div id="container">
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
        {% else %}
            <div>
                <a href="{{ path('fos_user_registration_register_expert') }}">{{ 'layout.sign_up'|trans }}</a>
                <a href="{{ path('home_page_seeker') }}">{{ 'layout.seeker_access'|trans }}</a>
            </div>
            <div>
                <a href="{{ path('hwi_oauth_service_redirect', {'service': 'linkedin' }) }}">{{ 'social.sign_in_linkedin'|trans }}</a> <br />
                <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}">{{ 'social.sign_in_google'|trans }}</a> <br />
            </div>
        {% endif %}

        </div>
    </div>
{% endblock %}
