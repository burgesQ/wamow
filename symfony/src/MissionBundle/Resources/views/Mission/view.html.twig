<html>
<body>
<h2>{{ mission.title }}</h2>
{% if mission.image is not null %}
  <img src="{{ mission.image }}">
{% endif %}

<div class="well">
  <p>
      <h4><br>Resume :</h4>
      {{ mission.resume }}

      <h5>details :</h5>
      {{ mission.professionalExpertise.name }}, {{ mission.missionKind.name }}
  </p>
  <p>
      <h4><br>Address :</h4>
      {{ mission.address.street }},
      {% if mission.address.state is not null %}
        {{ mission.address.street2 }},<br>
      {% endif %}
       {{ mission.address.city }},{{ mission.address.zipcode }}
       <br>{{ mission.address.country }}
       {% if mission.address.state is not null %}
        ,{{ mission.address.state }}<br>
       {% endif %}
   </p>
   <p>
       {% if mission.address.label is not null %}
        Label : {{ mission.address.state }}<br>
       {% endif %}
   </p>
   <p>
       {% if mission.address.name is not null %}
        Name : {{ mission.address.name }}<br>
       {% endif %}
   </p>
  <p>
      <h4><br>Daily Fees :</h4>
        {{ mission.dailyFeesMin }} $/days min <br>
        {{ mission.dailyFeesMax }} $/days max
  </p>
  <p>
      You have until {{ mission.applicationEnding|date('d/m/Y') }} to apply. <br>
      From {{ mission.missionBeginning|date('d/m/Y') }} to {{ mission.missionEnding|date('d/m/Y') }}<br>
  </p>

        {% if listLanguage is not null %}
          <h4><br>Language required :</h4>
          <ul>
            {% for language in listLanguage %}
              <li>{{ language.name }}</li>
            {% endfor %}
          </ul>
        {% endif %}
  <p>
        {% if mission.confidentiality == 1 %}
          This mission is confidential.
        {% endif %}
        {% if mission.telecommuting == 1 %}
          This mission require physical presence.
        {% endif %}
        {% if mission.international == 1 %}
          This mission is international.
        {% endif %}
  </p>
</div>
</body>
</html>
