{% extends "base.html.twig" %}
{% trans_default_domain "DashboardBundle" %}

{% block head %}
    {{ parent() }}
{% endblock %}

{% block subtitle %}
        Hello contractor
{% endblock %}
{% block body %}
    <p>
        Missions you created :
        {% for currentMission in listMission %}
        <li>
            <a href="{{ path('mission_view', {'id': currentMission.mission.id}) }}">
                {{ currentMission.mission.title }}</a>
        </li>
        {% endfor %}
    </p>
{% endblock %}
