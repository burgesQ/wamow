bundles:
    resource: "./routing_v1.yml"
    prefix: /{_locale}
    requirements:
        _local: "%app_locales%"

home_page:
    path:      /
    defaults:
        _controller: HomePageBundle:Default:expert
